---
subcategory: "{{ (index (split .Description "\n") 0) | plainmarkdown | trimspace }}"
page_title: "CTYUN: {{.Name}}"
---

# {{.Name}} ({{.Type}})

{{- if len (.Description | trimspace) }}
{{ (index (split .Description "\n") 1) | trimspace }}
{{- end }}



{{ if .HasExample -}}
## Example

{{tffile .ExampleFile }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}



{{- if .HasImport }}
## 导入

使用以下语法支持导入：

{{codefile "shell" .ImportFile }}
{{- end }}
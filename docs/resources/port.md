# ctyun_port (Resource)
-> 详细说明请见文档：https://www.ctyun.cn/document/10026730/10225195



## Example

```terraform
terraform {
  required_providers {
    ctyun = {
      source = "ctyun-it/ctyun"
    }
  }
}

provider "ctyun" {
  env = "prod"
}


resource "ctyun_vpc" "vpc_test" {
  name        = "vpc-test-ccse1"
  cidr        = "192.168.0.0/16"
  description = "terraform测试使用"
  enable_ipv6 = true
}

resource "ctyun_subnet" "subnet_test" {
  vpc_id      = ctyun_vpc.vpc_test.id
  name        = "subnet-test-ccse1"
  cidr        = "192.168.0.0/16"
  description = "terraform测试使用"
  dns = [
    "100.95.0.1"
  ]
  enable_ipv6 = true
}

resource "ctyun_security_group" "security_group_test" {
  vpc_id      = ctyun_vpc.vpc_test.id
  name        = "tf-sg-for-image"
  description = "terraform测试使用"
}
resource "ctyun_port" "port" {
  name                       = "port-test-update"
  description                = "port 测试-测试"
  subnet_id                  = ctyun_subnet.subnet_test.id
  security_group_ids         = [ctyun_security_group.security_group_test.id]
  secondary_private_ip_count = 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) 弹性网卡名称。支持拉丁字母、中文、数字，下划线，连字符，中文/英文字母开头，不能以http:/https:开头，长度2-32  支持更新
- `subnet_id` (String) 子网ID

### Optional

- `description` (String) 弹性网卡描述。支持拉丁字母、中文、数字, 特殊字符：~!@#$%^&*()_-+= <>?:{},./;'[]·~！@#￥%……&*（） —— -+={}|《》？：“”【】、；‘'，。、，不能以http:/https:开头，长度0-128 支持更新
- `ipv6_address_count` (Number) IPv6地址数量，指定IPv6地址数量，让系统为您自动创建IPv6地址，最多支持10个
- `ipv6_addresses` (List of String) IPv6地址列表，指定IPv6地址，不能和ipv6_address_count同时指定，最多支持10个
- `primary_ip_address` (String) 主私有IP地址，如果不指定则自动分配
- `region_id` (String) 资源池ID，如果不填则默认使用provider ctyun中的region_id或环境变量中的CTYUN_REGION_ID
- `secondary_private_ip_count` (Number) 辅助私有IP地址数量，指定私有IP地址数量，让系统为您自动创建IP地址，最多支持10个
- `secondary_private_ips` (Set of String) 辅助私有IP地址列表，指定私有IP地址，不能和secondary_private_ip_count同时指定，最多支持10个
- `security_group_ids` (Set of String) 安全组ID列表，最多支持10个

### Read-Only

- `id` (String) 网卡ID
- `instance_id` (String) 绑定的实例ID
- `instance_type` (String) 实例类型
- `mac_address` (String) MAC地址
- `port_id` (String) 网卡ID
- `status` (String) 网卡状态
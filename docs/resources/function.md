---
subcategory: "函数工作流（FunctionGraph）"
page_title: "CTYUN: ctyun_function"
---

# ctyun_function (Resource)
-> 管理函数



## Example

```terraform
terraform {
  required_providers {
    ctyun = {
      source = "ctyun-it/ctyun"
    }
  }
}

# 可参考 index.md，在环境变量中配置 ak、sk、资源池 ID、可用区名称
provider "ctyun" {
  env = "prod"
}

# 从 ZOS 桶上传代码包创建函数
resource "ctyun_function" "function_from_zos" {
  name                          = "tf-function-from-zos"
  create_type                   = 1
  runtime_runtime               = "Python3.9"
  runtime_handle_type           = "event"
  runtime_handler               = "index.handler"
  runtime_execute_timeout       = 60
  runtime_instance_concurrency  = 10
  container_time_zone           = "Asia/Shanghai"
  container_disk_size           = 512
  container_memory_size         = 256
  container_cpu                 = 0.25
  container_listen_port         = 8080
  description                   = "Terraform 创建的测试函数 - 从 ZOS 上传"
  code_bucket                   = "your-bucket-name"
  code_key                      = "your-function-code.zip"
  
  # 可选配置
  # environment = {
  #   ENVIRONMENT = "production"
  #   LOG_LEVEL   = "info"
  # }
  # role = "your-iam-role-name"
}

# 使用内联代码创建函数
resource "ctyun_function" "function_inline_code" {
  name                          = "tf-function-inline"
  create_type                   = 1
  runtime_runtime               = "Python3.9"
  runtime_handle_type           = "http"
  runtime_handler               = "main.handler"
  runtime_execute_timeout       = 30
  runtime_instance_concurrency  = 5
  container_time_zone           = "UTC"
  container_disk_size           = 512
  container_memory_size         = 128
  container_cpu                 = 0.1
  container_listen_port         = 8080
  description                   = "Terraform 创建的测试函数 - 内联代码"
  code_content                  = base64encode(file("${path.module}/function_code.zip"))
  
  # 自定义容器配置
  # container_max_scale = 100
  # container_fast_start = 1
  
  # 环境变量配置
  environment = {
    TF_MANAGED    = "true"
    CREATED_BY    = "terraform"
    PROJECT_NAME  = "faas-demo"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) 函数名称。长度限制：2~64 个字符，支持数字、字母、下划线、连字符，以字母开头
- `runtime_handler` (String) 函数执行的入口（标准运行时必填）支持更新
- `runtime_runtime` (String) 运行时类型，如 python3.9 支持更新

### Optional

- `code_bucket` (String) OBS 桶名称
- `code_content` (String) 内联代码内容或 Zip 文件的 Base64 编码
- `code_key` (String) OBS 对象键（文件路径）
- `code_version` (String) 代码版本号
- `container_cpu` (Number) CPU 规格 (vCPU)，默认 0.1，范围 0.05~12.0 支持更新
- `container_disk_size` (Number) 磁盘规格 (Mb)，默认 512，范围 512,10240,20480 支持更新
- `container_fast_start` (Number) 启动加速，默认为 0，1 表示使用启动加速
- `container_image` (String) 函数服务镜像 支持更新
- `container_listen_port` (Number) 监听端口，默认 8080 支持更新
- `container_max_scale` (Number) 并发实例数上限
- `container_memory_size` (Number) 内存规格 (Mb)，默认 128，范围 128~32768 支持更新
- `container_run_command` (String) 函数服务启动命令 支持更新
- `container_time_zone` (String) 时区，默认 UTC 支持更新
- `create_type` (Number) 创建函数的类型 1:标准运行时 2:自定义运行时 3:自定义镜像
- `description` (String) 函数描述，长度最大为 512，支持更新
- `environment` (Map of String) 函数环境变量，支持更新
- `region_id` (String) 资源池 ID，如果不填则默认使用 provider ctyun 中的 region_id 或环境变量中的 CTYUN_REGION_ID
- `role` (String) 角色 支持更新
- `runtime_execute_timeout` (Number) 执行超时时间，默认 10 秒 支持更新
- `runtime_handle_type` (String) 请求处理程序类型（标准运行时必填），如 http/event
- `runtime_instance_concurrency` (Number) 实例最大并发度 支持更新

### Read-Only

- `created_at` (String) 创建时间
- `function_id` (String) 函数唯一标识
- `id` (String) 资源唯一标识
- `status` (String) 函数状态
- `updated_at` (String) 更新时间
## 导入

使用以下语法支持导入：

```shell
# 导入函数资源
#[] 标记的参数为必填参数
#<> 标记的参数为可选参数，不填则取值环境变量值
terraform import ctyun_function.[导入配置名称] [name][,<region_id>]
# 示例 1: 只指定函数名称，region_id 从 provider 或环境变量获取
terraform import ctyun_function.function_from_zos tf-function-from-zos
# 示例 2: 同时指定函数名称和 region_id
terraform import ctyun_function.function_from_zos tf-function-from-zos,200000002401
```
# ctyun_kafka_consumer_groups (Data Source)
-> 详细说明请见文档：https://www.ctyun.cn/document/10029624/10145103



## Example

```terraform
terraform {
  required_providers {
    ctyun = {
      source = "ctyun-it/ctyun"
    }
  }
}

# 可参考index.md，在环境变量中配置ak、sk、资源池ID、可用区名称
provider "ctyun" {
  env = "prod"
}

resource "ctyun_kafka_instance" "test" {
  instance_name     = "tf-kafka-34ywerkb"
  engine_version    = "3.6"
  spec_name         = "kafka.4u8g.cluster"
  node_num          = 5
  zone_list         = ["cn-huadong1-jsnj1A-public-ctcloud"]
  disk_type         = "SSD"
  disk_size         = 300
  vpc_id            = "vpc-ewivt5nhiz"
  subnet_id         = "subnet-vhyywu7mfe"
  security_group_id = "sg-ed9i3c98t2"
  cycle_type        = "on_demand"
  retention_hours   = 55
}

data "ctyun_kafka_consumer_groups" "test" {
  name        = "init-kafka-consumer_group"
  instance_id = ctyun_kafka_instance.test.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `instance_id` (String) 实例ID
- `name` (String) 消费组名称，模糊查询

### Optional

- `page_no` (Number) 分页中的页数，默认1，范围1-40000
- `page_size` (Number) 分页中的每页大小，默认10，范围1-40000
- `region_id` (String) 资源池ID

### Read-Only

- `consumer_groups` (Attributes List) 消费组列表 (see [below for nested schema](#nestedatt--consumer_groups))
- `total` (Number) 消费组总数

<a id="nestedatt--consumer_groups"></a>
### Nested Schema for `consumer_groups`

Read-Only:

- `coordinator_id` (Number) 协调器编号
- `create_time` (String) 创建时间，为UTC格式
- `description` (String) 消费组描述
- `id` (Number) 消费组ID
- `name` (String) 消费组名称
- `state` (String) 消费组状态
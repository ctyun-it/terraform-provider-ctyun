---
subcategory: "分布式消息服务RocketMQ"
page_title: "CTYUN: ctyun_rocketmq_groups"
---

# ctyun_rocketmq_groups (Data Source)
-> 查询 RocketMQ 订阅组列表



## Example

```terraform
terraform {
  required_providers {
    ctyun = {
      source = "ctyun-it/ctyun"
    }
  }
}

provider "ctyun" {
  env = "prod"
}

# 创建 VPC
resource "ctyun_vpc" "example" {
  name        = "tf-vpc-for-rocketmq"
  cidr        = "192.168.0.0/16"
  description = "terraform测试使用"
  region_id   = "200000001703"
}

# 创建子网
resource "ctyun_subnet" "example" {
  name        = "tf-subnet-for-rocketmq"
  cidr        = "192.168.0.0/16"
  description = "terraform测试使用"
  vpc_id      = ctyun_vpc.example.id
  region_id   = "200000001703"
  dns         = ["8.8.8.8", "8.8.4.4"]
}

# 创建安全组
resource "ctyun_security_group" "example" {
  name        = "tf-sg-for-rocketmq"
  description = "terraform测试使用"
  vpc_id      = ctyun_vpc.example.id
  region_id   = "200000001703"
}

# 查询可用区
data "ctyun_zones" "test" {
  region_id = "200000001703"
}

# RocketMQ 实例
resource "ctyun_rocketmq_instance" "example" {
  instance_name     = "tf-rocketmq-instance"
  spec_name         = "rocketmq.4u8g.cluster"
  node_num          = 4
  zone_list         = data.ctyun_zones.test.zones
  disk_size         = 100
  disk_type         = "SAS"
  vpc_id            = ctyun_vpc.example.id
  subnet_id         = ctyun_subnet.example.id
  security_group_id = ctyun_security_group.example.id
  cycle_type        = "month"
  cycle_count       = 1
  region_id         = "200000001703"
}

# 查询 RocketMQ 实例下的所有订阅组
data "ctyun_rocketmq_groups" "test" {
  instance_id = ctyun_rocketmq_instance.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `instance_id` (String) RocketMQ 实例 ID

### Optional

- `name` (String) 订阅组名称，支持模糊查询
- `region_id` (String) 资源池 ID

### Read-Only

- `groups` (Attributes List) 订阅组列表 (see [below for nested schema](#nestedatt--groups))

<a id="nestedatt--groups"></a>
### Nested Schema for `groups`

Read-Only:

- `broker_id` (Number) Broker ID
- `broker_name` (String) Broker 名称
- `consume_enable` (Boolean) 是否允许消费
- `first_consume_mechanism` (Number) 首次消费机制（0:从最小位点开始消费，1:从最新位点开始消费）
- `name` (String) 订阅组名称
- `notify_consumer_ids_changed_enable` (Boolean) 是否开启消费者 ID 变更通知
- `pull_mechanism` (Number) 拉取机制（0:服务器主动推送，1:客户端拉取）
- `remark` (String) 备注信息
- `retry_max_times` (Number) 最大重试次数
- `retry_queue_nums` (Number) 重试队列数量
- `which_broker_when_consume_slowly` (Number) 消费慢时指定的 Broker ID